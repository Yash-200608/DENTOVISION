## Summary

<!-- What changed and why. -->

## Test plan

- [ ] `pytest` passes locally
- [ ] Manual check of `/health` and `/predict` if API behavior changed

## Checklist

- [ ] No secrets, `.env` files, or credentials
- [ ] No patient data, DICOM files, or PHI in the diff or screenshots
- [ ] Imports use the `dentovision.*` package (`src/` layout)
- [ ] Docs updated if commands, env vars, or endpoints changed
